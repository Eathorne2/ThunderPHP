# Array Column

Extracts one value from each row of an array or Traversable result. It supports rows represented as objects or associative arrays.

Dot notation is supported, for example `user.name`.
