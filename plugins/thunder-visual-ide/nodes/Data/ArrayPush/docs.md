# Array Push

Connect an existing **Array** and one **Value** from a result, variable, request value, model call, or any other compatible node. The node returns a new array with the value appended using normal PHP `$items[] = $value` behavior.

Connect **Updated Array** to an Assign Variable node when the updated value needs to replace an existing variable.
