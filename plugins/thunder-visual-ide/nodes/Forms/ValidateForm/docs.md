# Validate Form Schema

Builds validation rules from a selected visual Form schema and validates connected form data through `Core\Validate`.

The **Extra or replacement validation rules** manager uses the same rule catalogue as the normal Validate node. Rules entered here are merged over automatically derived form-field rules, allowing fields to be extended or replaced without remembering rule syntax.

Custom rules and raw JSON remain supported.
