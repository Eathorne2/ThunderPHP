# Visual Folder

Collapsible editor-only group. Ignored by the compiler.

When a folder is collapsed, its member nodes and their connection lines are hidden. Expanding the folder restores the original nodes and connections exactly as they were.

Dragging a folder moves the folder and all of its member nodes together. Folder grouping does not rewrite graph connections or affect generated code.
