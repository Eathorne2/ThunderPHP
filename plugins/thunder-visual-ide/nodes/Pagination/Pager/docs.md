# Create Pager

Creates `\Core\Pager($itemsPerPage, $extraLinks, $totalRows)`. Connect the **Limit** and **Offset** outputs to model method parameters, and keep this node in the execution path before the query.
