# Chart Dataset

Creates one Chart.js dataset as a PHP array. Connect an Array Column, Array, query result transformation, or another array-producing node to **Data**.

Use the color inputs when a chart needs one color per value, such as pie or doughnut charts. Leave them disconnected to use the configured defaults.

The **Advanced Dataset Options** array is merged last and may override any generated option.
