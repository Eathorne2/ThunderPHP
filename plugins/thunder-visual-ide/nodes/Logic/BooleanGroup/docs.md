# Combine Conditions

Combines two or more boolean expressions for an If node.

- **AND** compiles to `&&`; every connected condition must be true.
- **OR** compiles to `||`; one connected condition must be true.

Increase the condition count to expose more boolean input sockets. Combine nodes can be chained to create nested expressions.
