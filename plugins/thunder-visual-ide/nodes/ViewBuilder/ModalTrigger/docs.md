# Modal Trigger

Connect **Modal Target** to a Modal's **Triggers** input. The first element in Trigger HTML receives the selected `open`, `close` or `toggle` action.

The node can also be used as a normal child or as a reusable visual component through its component name. A selected Modal fallback is available when a target connection is inconvenient; the visual connection takes priority.
