ThunderPHP uses the `Core\Validate` class to validate incoming data.

It supports:

- built-in validation rules
- custom rules through hooks
- file and image validation
- advanced uniqueness checks with `where` and `whereNot`

This page covers the common validation flow, available rules, advanced uniqueness options, and how to extend the validator without editing the core class.
